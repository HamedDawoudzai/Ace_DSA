# Production environment for EAS builds.
# EAS injects EXPO_PUBLIC_* vars at build time via eas.json env blocks,
# but this file serves as documentation and a local override.

EXPO_PUBLIC_API_BASE_URL=https://api.acedsa.app
